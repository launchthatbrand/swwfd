"use client";

import type { ReactNode } from "react";

type RecordFieldEditDialogsProps = {
  children: ReactNode;
};

export const RecordFieldEditDialogs = ({ children }: RecordFieldEditDialogsProps) => {
  return <>{children}</>;
};
