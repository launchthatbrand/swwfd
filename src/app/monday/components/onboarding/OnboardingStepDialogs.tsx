"use client";

import type { ReactNode } from "react";

type OnboardingStepDialogsProps = {
  children: ReactNode;
};

export const OnboardingStepDialogs = ({ children }: OnboardingStepDialogsProps) => {
  return <>{children}</>;
};
